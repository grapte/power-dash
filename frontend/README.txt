How to hack this frontend:

`npm install` should get you everything you need given a reasonable node environment.

Use `npm run dev` for development, and `npm run build` when building the docker image.